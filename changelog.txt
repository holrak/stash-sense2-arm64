## 2026-09-24

### 0.27.2 (plugin)
- Fix: the performer photo in an Upstream Performer Changes detail view was
  cropped at the top and bottom; it now shows at its real proportions.

### 0.27.1 (plugin)
- Fix: a match whose performer record wasn't found (source such as "seekfans")
  showed a dead "View on seekfans" link; it now shows the source as plain text.

### 0.35.0 (sidecar) / 0.27.0 (plugin)
- Improvement: the retired gender/age model is no longer offered for download
  (or in Download All); if still installed, Settings > Models shows it as
  "No longer used" with a Delete button. Plugin now requires sidecar 0.35.0+.

## 2026-09-23

### 0.34.7 (sidecar)
- Fix: removed the buffalo_l gender/age model and its soft gender-mismatch
  match penalty -- the model's predictions were unreliable and were
  degrading match confidence for correct candidates.

## 2026-09-14

### 0.34.6 (sidecar) / 0.26.2 (plugin)
- Fix: a local performer added from a catalogue-only source (e.g.
  javdatabase.com) only showed "View local performer" on a match card,
  never a "View on <site>" link, unlike a StashDB-linked local performer.

### 0.34.5 (sidecar)
- Fix: linked performer records (data-gen link groups, and local performers
  linked to a StashDB entry) showed as duplicate/split entries instead of
  merging, in Face Recommendations and scene identify.
- Fix: a linked group's display candidate now follows configured stash-box
  endpoint priority (falling back to default connection order), not raw
  match score.
- Fix: max_distance filtering now runs before collapsing a linked group, so
  an out-of-threshold candidate can no longer win display and then get
  discarded.
- Fix: cluster merging now uses each cluster's own dominant identity instead
  of a naive transitive union, avoiding both under- and over-merging.

### 0.34.4 (sidecar)
- Fix: a local-index match and the main database's own StashDB entry for the
  same real person still showed as two separate "persons" -- scene-level
  cross-frame tallying now resolves a local match's linked stashdb_id first,
  same as the single-face candidate list already did.

### 0.34.3 (sidecar)
- Fix: 0.34.2's endpoint-priority display pick had no effect when no custom
  order was ever explicitly saved -- now falls back to default connection
  order for unranked endpoints, matching the Settings UI's own fallback.

### 0.34.2 (sidecar)
- Fix: a merged linked group with multiple candidate records could show a
  catalogue source (e.g. pornbox) outranking the same person's stashdb.org
  record. Now prefers configured endpoint priority, falling back to match
  score only when no candidate has a priority endpoint.

### 0.34.1 (sidecar)
- Fix: two face clusters matching data-gen-linked records of the same real
  person showed as two separate low-frame "persons" instead of one --
  scene-level tallying (frequency/aggregate/cluster-merge/hybrid) now
  canonicalizes through performer_link_index like single-face matching
  already did.

### 0.26.1 (plugin)
- Fix: face-match thumbnails were center-cropping tall portrait photos,
  cutting off the subject's head. Crop now anchors to the top instead.

### 0.26.0 (plugin)
- Feature: the changelog button now opens a 3-tab view ("What's New" plus
  separate Sidecar/Plugin history tabs) instead of one long combined list.

## 2026-09-13

### 0.34.0 (sidecar)
- Feature: `GET /release/changelog` -- full sidecar+plugin changelog
  history, for the new plugin changelog button, regardless of what's
  currently installed.
- Fix: `/health`/`/database/info` reported face_count from faces.json's
  sparse array length (grows forever, never shrinks) instead of the real
  loaded index size -- ~193k-face gap on one deployment.
- Fix: recommendation list queries ordered by created_at alone (1s
  resolution), so ties could return a different row across fetches, making
  Face Recommendations cards appear to shuffle. Added an id DESC tiebreak.
- Fix: opening a Face Recommendations scene whose representative candidate
  had itself been dismissed hid the scene's still-pending candidates
  entirely. Now checks for any pending candidate first.

### 0.25.0 (plugin)
- Feature: a changelog button in the dashboard header opens the full
  sidecar+plugin changelog on demand (needs sidecar 0.34.0+).
- Improvement: the Identify status dot no longer turns blue for "newer
  optional release available" (confusing, looked like an error) -- that
  signal moved to a per-component indicator in the header instead.
- Fix: dismissing/undismissing a Face Recommendations candidate never
  updated its scene's cached list card until a full refresh.

## 2026-09-11

### 0.24.2 (plugin)
- Fix: the dashboard's page-jump dropdown no longer lists every page (could
  reach into the thousands) -- windowed to +-15 pages around the current one.

## 2026-09-09

### 0.33.0 (sidecar)
- Improvement: face delta rows now apply upsert-safe (update in place, not
  insert-only), since deltas can now correct an already-published face's
  gender/age/vector, not just add new ones. Requires this version or newer
  to apply such a delta (older sidecars fall back to a full download).
- The separate face_field_updates delta path is removed, superseded by the
  upsert-safe handling above.

## 2026-09-08

### 0.32.2 (sidecar)
- Fix: a query face needing roll correction had its gender/age predicted
  against the wrong, unrotated frame -- now uses the actual rotated frame
  detection ran against.
- Fix: the roll-correction loop re-rotated its own already-rotated output on
  each retry, compounding blur -- now always rotates the original image by
  the cumulative angle.

### 0.32.1 (sidecar) / 0.24.1 (plugin)
- Fix: accepting a Scene Stash-Box Tagger match could leave a sibling
  candidate for the same scene stuck showing pending (server-side
  auto-dismissed, but not reflected in the plugin's list cache). The accept
  endpoint now also returns auto-dismissed ids for the plugin to purge.

## 2026-09-06

### 0.32.0 (sidecar) / 0.24.0 (plugin)
- Fix: match thumbnails were fixed to a portrait box, cropping landscape
  source images (video frame captures). Now swaps to a landscape box when
  the image itself is landscape.
- Feature: the "pick existing performer or create new" collision modal now
  shows the original match's own photo and source link, not just a bare name.

### 0.31.2 (sidecar only)
- Fix: creating a performer from a catalogue match failed with a 500 when
  the source photo (e.g. iafd.com) blocked Stash's own fetch. Now retries
  performer creation once without the image instead of failing outright.

### 0.23.2 (plugin only)
- Fix: job history's "Finished in" duration counted time spent queued
  (waiting on a resource conflict) as run time. Now measured from
  started_at instead of created_at.

### 0.31.1 (sidecar only)
- Fix: 0.31.0's id-collision repair only moved a clobbered performer's own
  metadata back, not a face that had been silently fused onto the same row.
  Delta.db can now carry a face_owner_repair table to reassign such faces to
  their true owner (needs data-gen's min-sidecar-version 0.31.1 release).

### 0.31.0 (sidecar only)
- Fix: a locally-autoincremented stashbox performer id could collide with a
  server-assigned catalogue performer id, silently overwriting the
  stashbox performer's data. Stashbox deltas now carry the server's
  permanent id, and every id-targeted write relocates a colliding occupant
  first instead of overwriting it.

### 0.23.1 (plugin only)
- Fix: accepting/resolving a recommendation from its detail view often left
  it looking un-removed in the pending list, since the auto-return helper
  captured which id to drop too late. Now captures it immediately.

## 2026-09-05

### 0.30.0 (sidecar only)
- Feature: face matches now collapse a data-gen-detected "same person,
  different unlinked identity" group to a single candidate, preferring the
  member with the highest-priority stash-box source over a marginally
  closer catalogue match -- avoids creating duplicate performers on Accept.

## 2026-09-03

### 0.29.0 (sidecar only)
- Feature: local performer sync now uses a face-quality signal (CR-FIQA) to
  catch a cover photo where the largest detected box was actually a
  misidentified non-face object, not the real subject.

### 0.28.0 (sidecar only)
- Fix: accepting a Face Recommendations match whose local performer had
  already been deleted/merged in Stash crashed with an unhandled 500. Now
  validates each performer id first: a stale one is dismissed (with its
  scene rematched) instead of crashing the request.

### 0.23.0 (plugin only)
- Fix: the Face Recommendations accept button showed "Accepted!" even when
  the backend reported the new stale-performer failure above. Now shows a
  clear error toast instead.

## 2026-09-02

### 0.27.0 (sidecar only)
- Fix: face detection now corrects for in-plane rotation ("roll") -- a
  photo/frame that's genuinely sideways or upside-down degraded recognition
  badly past ~60-90 degrees despite high detector confidence. Iterates
  rotate+re-detect up to 3 times until roll converges near zero.

### 0.22.0 (plugin only)
- Fix: "Select to identify" now pads the user's drawn selection by 40% on
  each side before sending it -- a tightly-drawn selection reliably found no
  face at all (the detector needs surrounding context).

### 0.26.0 (sidecar only)
- Feature: delta packages can now remove a fully-disabled catalogue
  performer via a new catalogue_removed_performers table -- additive and
  backward-compatible with older sidecars/deltas.

### 0.25.2 (sidecar only)
- Fix: pinned onnx<1.18, added ml_dtypes<0.5, and tightened usearch<2.25 in
  requirements -- unpinned versions reliably segfaulted on startup.

## 2026-08-30

### 0.21.0 (plugin only)
- Improvement: recommendations list pagination is now client-side -- one
  fetch per tab, with Next/Prev/jump slicing already-fetched data instead of
  re-querying the backend on every click (~2s delay removed).
- Improvement: new pagination control (first/prev/page-jump/next/last)
  replacing plain Prev/Next.
- Fix: opening a recommendation type no longer inherits the status
  tab/page last viewed on a different type -- always starts at Pending,
  page 1.
- Fix: returning to the list now restores scroll position instead of
  landing back at the top.
- Fix: Face Recommendations detail cards now use the same rectangular
  thumbnail as manual Identify results, instead of a cropping circular one.
- Fix: catalogue-match links now read "View on <site>" like manual Identify
  already does.
- Fix: a match with a known source but no profile URL now shows "Source:
  <name>" as plain text instead of no source info at all.
- Improvement: match photos now load a source-specific thumbnail (StashDB's
  own resize param, seekfans' image proxy) instead of the full-size
  original, cutting bandwidth.

### 0.25.1 (sidecar only)
- Fix: creating a performer from a catalogue source double-wrapped the
  source name in brackets for disambiguation (Stash's UI already adds
  them) -- now writes the bare name.

### 0.20.4 (plugin only)
- Improvement: dismissing a candidate from the pending list now toggles the
  button to Undismiss in place instead of just removing it.

### 0.20.3 (plugin only)
- Fix: in a scene_face_match detail view, every person after the first
  showed "Undismiss" regardless of actually being pending -- a map()
  callback was leaking the array index into a boolean parameter. Confirmed
  live.

### 0.20.2 (plugin only)
- Fix: a dismissed candidate in "Show Dismissed" could render pre-selected
  even though its checkbox is disabled -- could silently ride along into
  Accept Selected. Dismissed candidates are never pre-selected now.
- Improvement: the "Show Dismissed" toggle and panel now sit above Accept
  Selected/Reject All instead of below.

### 0.25.0 (sidecar) / 0.20.1 (plugin)
- Fix: any local performer sync forced a full unload+reload of the entire
  face recognition resource group, independent of the Idle Unload Timeout
  setting. New reload_local_performer_index() refreshes just the local
  index in place.
- Fix: the live single-frame identify view could show "Add to Scene" for a
  performer already tagged on the scene.
- Fix: live identify views showed a noisy "No match found" card for every
  unmatched face even when others in the same result did match. Only
  matched faces get a card now.

### 0.24.0 (sidecar) / 0.20.0 (plugin)
- Feature: Face Recommendations gained a "Show Dismissed" toggle with
  per-candidate Undismiss, since a dismissal was previously invisible and
  irreversible without deleting data. Requires sidecar 0.24.0+.
- Fix: the live Identify results view could show "Add to Scene" for an
  already-tagged performer when the cached result predates the tagging.

## 2026-08-29

### 0.23.0 (sidecar only)
- Improvement: sprite-tile face detection now sizes the detector's canvas
  to each tile's actual dimensions instead of forcing a fixed 640x640 --
  ~3x faster overall with a small accuracy gain.
- Feature: new "Use Sprite Tiles For Detection" setting (on by default) to
  disable sprite-tile detection for background scans.

### 0.22.0 (sidecar) / 0.19.0 (plugin)
- Fix: accepting a match could silently link the wrong performer whenever a
  name/alias matched an unrelated existing local performer -- name/alias
  alone is no longer sufficient evidence; anything besides a real stash_id
  or URL match now blocks and prompts via a new card picker.
- Add: linking/creating from the new confirmation flow now writes the
  stash_id/URL onto the performer so future scenes resolve automatically.
- Add: performer creation from the flow now handles catalogue-sourced
  candidates (seekfans, pornbox) correctly, without stash_ids.
- Add: a local performer's own stored URLs are now cross-checked against
  catalogue candidates during identification.
- Add: opening a Face Recommendations scene now does a live stash_id
  lookup for real-StashDB candidates missing a local link.
- Fix: Face Recommendations' "jump to frame" buttons went silently empty
  under matching_mode="hybrid" and for sprite-tile faces sharing one frame
  index. Both fixed; already-cached data repaired automatically.

### 0.21.3 (sidecar only)
- Add: sprite-tile face embeddings are now cached instead of re-detected on
  every use -- same treatment video-frame embeddings already had.
- Fix: Refresh Outdated now always requests sprite coverage (cheap now
  that detection is cached, not per-bulk-run).
- Fix: Face Recommendations' full-scan mode now does a real rematch against
  current performer data instead of reading stale stored data.

### 0.21.2 (sidecar only)
- Fix: local performer sync's progress/ETA got stuck behind the slowest
  in-flight performer -- added a separate real-completion counter.
- Fix: local performer sync took 5+ minutes even with nothing changed --
  removed a redundant per-performer re-fetch and reduced progress-commit
  frequency (1587 performers: 5m19s -> 10s).

### 0.18.1 (plugin only)
- Fix: long scene titles with no spaces didn't wrap in Face Recommendations
  cards -- break-point helper extended to also break on `.`/`,`.
- Fix: the scene-review video preview showed no cover image before
  playback (missing poster attribute).
- Add: pagination controls now also render above the recommendation list,
  not just below.

## 2026-08-28

### 0.21.1 (sidecar only)
- Fix: a live /identify request running concurrently with a GPU batch job
  could hang or crash (real ROCm/MIOpen failures) -- live endpoints
  bypassed the job queue's GPU concurrency limit entirely. Added a
  process-wide lock around actual GPU inference calls.
- Fix: a face-detection worker thread that raised died silently, leaving
  its frame's result as None forever and corrupting a later, unrelated-
  looking crash. Now captured and re-raised.

### 0.21.0 (sidecar) / 0.18.0 (plugin)
- Fix: "Fingerprint Missing" and "Refresh Outdated" were the same job type
  with scope hidden inside its resumption cursor, so the UI's busy state
  and Quick Action were ambiguous about which scope would run. Split into
  two real, independently-tracked job types.

### 0.20.0 (sidecar) / 0.17.0 (plugin)
- Feature: "Identify full video" now checks for a stored Face
  Identification result first and renders it instantly instead of always
  re-running detect+embed+match.
- Feature: added a "Re-identify" action to force a fresh pass when needed
  (e.g. after tagging a performer or a database update).

### 0.19.0 (sidecar) / 0.16.3 (plugin)
- Feature: Face Recommendations (renamed from Scene Face Matches) no
  longer re-runs detect+embed+match itself -- it reads Face
  Identification's stored data, with a small on-demand sprite top-up only
  for scenes still missing sprite coverage.

### 0.18.0 (sidecar only)
- Feature: scene identification now stores the full per-person candidate
  match list, not just the best match, in a new scene_fingerprint_matches
  table -- first step toward one shared source of truth for both
  recommendations and the live Identify button.
- Feature: Face Identification (renamed from Fingerprint Generation) now
  runs with hybrid matching, sprite tiles, and top_k=5 by default, matching
  what the live Identify button already used.
- Migration note: existing fingerprint summaries are dropped, rebuilt on
  next (re-)identify -- no re-detection needed, just cheap re-matching.

### 0.17.1 (sidecar) / 0.16.2 (plugin)
- Fix: fingerprint generation submitted with no cursor (Quick Actions, or a
  scheduled run) fell back to a full refresh instead of the safe
  missing-only default -- bypassing the earlier Missing/Outdated split.

### 0.16.1 (plugin only)
- Fix: moved the "Outdated"/"Refresh Outdated" stat and button from the
  fingerprint-coverage row to the performer-database row, since outdated
  means the database is stale, not this scene's own fingerprint.

### 0.17.0 (sidecar) / 0.16.0 (plugin)
- Feature: scene fingerprinting now actually uses its cached-signal fast
  path -- a performer-database bump no longer requires redoing full
  detection/embedding for every scene, only cheap re-matching. Restored the
  disabled cache write path from the old dual-model era (schema v14).
- Feature: "Fingerprint Missing" and "Refresh Outdated" are now separate,
  explicit, cursor-respected actions instead of one button that silently
  reprocessed the whole library on every database bump.
- Fix: fingerprint generation pre-marked an entire fetched page as errored
  before any scene in it was attempted, visibly regressing the coverage
  count. Now marks each scene lazily, right before its own work begins.

### 0.16.1 (sidecar only)
- Feature: delta updates can now carry field-only changes to faces that
  already shipped in an earlier release (e.g. backfilled gender/age),
  applied as a minimal-column UPDATE, tolerant of version skew either way.

### 0.16.0 (sidecar only)
- Feature: local performer sync now runs as an async fetch pool + threaded
  embed pool + one writer instead of a strictly sequential loop.
- Feature: scene identification's per-frame detection loop now spreads
  work across a small pool of warm embedding-generator instances.
- Feature: face recognition now loads in the background right after
  startup instead of lazily on the first request.
- Feature: FFMPEG_HWACCEL=vaapi is now a per-scene decision (>=4K only),
  not global -- mixing VAAPI decode with ROCm compute destabilized the
  sidecar; >=4K scenes are also now decoded in small batches to keep the
  two off the GPU at the same moment.
- Feature: recommendations referencing a deleted/merged scene, performer,
  studio, or tag are now cleaned up automatically via new destroy hooks.
- Fix: the AMD ROCm HSA_OVERRIDE_GFX_VERSION guidance was wrong for
  gfx1103 (Radeon 780M) -- 11.0.0 is the stable value, not the literal chip id.

### 0.15.0 (plugin only)
- Feature: new Scene/Studio/Tag destroy hooks for recommendation cleanup,
  matching the sidecar entry above. Fixed a dispatcher bug that had been
  routing every hook to the performer handler unconditionally.

## 2026-08-27

### 0.15.1 (sidecar only)
- Fix: performer names with HTML entities (e.g. "Ellie &amp;lt;3") showed
  the raw entity text instead of the decoded character -- fixed at the
  crawl source, both export paths, and backfilled onto already-cached data.
- Fix: the scene-face-match video preview had no fallback source, so any
  scene with a codec the browser can't decode natively failed to play even
  though Stash's own player handled it fine. Now builds multiple <source>
  candidates the same way Stash's player does.

### 0.15.0 (sidecar only)
- Feature: minimum-sidecar-version compatibility gate for database
  updates -- a release embeds a min-sidecar-version marker, and an update
  is rejected server-side if the running sidecar is below it.
- Feature: soft steep-angle confidence penalty for face matching -- a
  reference photo shot from a steep angle no longer causes inflated
  false-positive matches. Scales from no penalty at 45 degrees down to
  0.5x confidence at a full 90-degree profile.

### 0.14.22 (sidecar only)
- Feature: soft gender-mismatch confidence penalty -- when the query
  face's predicted gender and a candidate's known/inferred gender
  confidently disagree, that candidate's confidence is multiplied down
  (never excluded outright).

### 0.14.21 (plugin only)
- Fix: the "Add as..." search modal showed a broken image for every result
  -- the sidecar's absolute image URL used its own origin, not the
  browser's. Now stripped to a relative path.

### 0.14.21 (sidecar only)
- Fix: the Scene Face Matches full-scan loop only refreshed the face
  recognition model once before the loop, not per-scene -- a scan crossing
  the idle-unload timeout silently failed most of its remaining scenes
  (72% failed on a real 1144-scene run). Now re-checked every scene.

### 0.14.20 (sidecar only)
- Fix: the recommendation list's pre-selected "best match" pick could still
  show elsewhere in sort order than first, since ranking wasn't aware of
  which candidate was actually pre-selected. Now always sorts the
  pre-selected candidate first.

### 0.14.19 (sidecar only)
- Fix: aggregate_matches (Scene Face Matches' matching_mode="cluster")
  still selected the "best" match using an old averaged score even after
  display switched to best-frame distance -- selection, display, and sort
  order now all use the same formula.

### 0.14.18 (sidecar only)
- Fix: deleting a scene_face_match group only pruned the clicked
  representative row, leaving sibling rows pending so the group reappeared
  on next load. Fixed with a whole-group prune.
- Fix: scene face-match identification displayed a cluster's *average*
  frame distance instead of the *best* frame's, which had actually decided
  the match -- a correct match with many tracked frames could show 0%
  confidence. Now shows the best frame's own distance.

### 0.14.17 (sidecar only)
- Feature: the sidecar now self-checks GitHub hourly for the latest
  sidecar/plugin releases, served via the existing /health poll with
  per-side changelog excerpts.
- MIN_PLUGIN_VERSION added, mirroring the sidecar-version floor the plugin
  already enforced.

### 0.14.16 (sidecar only)
- Fix: /database/check-update unconditionally forced a live GitHub call,
  bypassing the existing cache and burning through GitHub's rate limit
  within a day. Now defaults to the cache; only "Refresh" forces a check.

### 0.14.20 (plugin only)
- Feature: the connection dot now distinguishes four states (connected,
  disconnected, required mismatch, optional update available).
- Feature: a required version mismatch now pops up a modal automatically
  -- dismissable if the plugin is outdated, blocking if the sidecar is.

### 0.14.19 (plugin only)
- Fix: the Settings tab's database section now only forces a fresh GitHub
  check on an explicit Refresh click, not on every tab switch.

### 0.14.15 (sidecar only)
- Fix: the local performer create/update/destroy hook constructed
  LocalPerformerIndex with a leftover 3-arg (dual-model era) signature,
  raising AttributeError on every call. Fixed to the correct 2-arg form.
- Feature: GET /recommendations/local-performers/stats for the new
  Settings stat box.

### 0.14.18 (plugin only)
- Feature: Settings tab gains a "Local Faces" stat box for the local
  performer index, separate from the downloaded external database counts.

### 0.14.14 (sidecar only)
- Fix: applying a delta update blocked the entire event loop for its whole
  duration, including its own status poll -- surfaced as a false "Error"
  in the UI for an update that was actually still completing successfully.
  Moved to a worker thread.
- Improvement: real per-phase progress reporting for delta updates instead
  of a single jump from "downloading" to done.

### 0.14.13 (sidecar only)
- Fix: delta_applier.py only applied performer/face changes with a
  stash-box link -- a catalogue-only source's performers (pornbox,
  seekfans, legacy crawls) were silently dropped by a delta update. Added
  a second pass for catalogue tables; export_db_to_json.py fixed to match.

## 2026-08-25

### 0.14.12 (sidecar only)
- Feature: new "Scene Face Matches" job -- scans scenes with zero
  performers using the same identify pipeline the manual Identify button
  uses, one recommendation per (scene, candidate) pair keyed by performer
  identity so accept/dismiss survives future re-clustering.
- Feature: match responses now include up to 4 timestamps of a candidate's
  strongest frames (plain-clustering mode only).

### 0.14.17 (plugin only)
- Feature: new "Scene Face Matches" recommendations card -- per-person
  candidate columns with jump-to-timestamp buttons; a candidate is
  pre-selected only if it's the best match, appeared 5+ frames, and scored
  10%+ confidence.
- Fix: recommendation tab counts (Pending/Resolved/Dismissed) went stale
  after accepting/dismissing from a detail view -- now always fetched
  fresh, decoupled from the row-list cache.

## 2026-08-24

### 0.14.16 (plugin only)
- Feature: Duplicate Scenes pending list now supports bulk-dismiss via
  checkboxes ("Dismiss Selected"), alongside the existing "Dismiss All".

### 0.14.15 (plugin only)
- Fix: 0.14.13's stale-cache-page fix only fired past page 0, missing the
  most common case (page 0 fully cleared still showing "No pending
  recommendations"). Now re-fetches whenever the current page comes back
  empty with a nonzero total, on any page.

### 0.14.14 (plugin only)
- Feature: Upstream Scene Changes now offers an "Update scene cover"
  checkbox (ticked by default) alongside the rest of the change.

### 0.14.13 (plugin only)
- Fix: the recommendations list showed a false "No pending recommendations"
  on any page past the first once every entry on it had been surgically
  removed, even with pending entries on earlier pages. Now snaps back to
  the last valid page and re-fetches.

## 2026-08-23

### 0.14.11 (sidecar only)
- Fix: resolving a StashBox parent-studio UUID to a local studio could
  resolve a studio's parent right back to itself, failing Stash's own
  self-referential-ancestor check with no clear reason. Now excludes the
  studio being resolved for from the lookup.

### 0.14.12 (plugin only)
- Fix: the list kept showing pre-bulk-action counts/cards after any "Accept
  All"/"Dismiss All" bulk action -- those handlers refreshed the view but
  never invalidated the cache.
- Fix: "Accept All Changes" silently skipped recommendations with a real
  diff that just smart-defaults to "keep local" -- these are now resolved
  as accepted_no_changes instead of staying pending forever (27 of 30
  affected on one real run).

### 0.14.10 (sidecar only)
- Fix: "Accept All Changes" for Upstream Studio Changes processed every
  pending item but applied none -- the update endpoint never translated
  the `_alias_add` meta-key into Studio's real `aliases` field. Now merges
  it in like the tag/performer endpoints already did.

### 0.14.11 (plugin only)
- Improvement: returning from a recommendation detail view to its list no
  longer does a full re-fetch -- the last-fetched page is now cached, with
  surgical single-item removal on accept/dismiss/resolve.

### 0.14.9 (sidecar only)
- Fix: AMD GPU VRAM detection picked the first sysfs card entry it found --
  on a board with both an iGPU and dGPU, this could report the iGPU's tiny
  framebuffer instead of the dGPU's real VRAM. Now takes the max across
  every card.

### 0.14.8 (sidecar only)
- Fix: Stop/Force Cancel didn't actually stop most analysis-based queue
  jobs -- the stop-signal bridge only snapshotted state before the job
  started running. Now a live delegation, plus each analyzer's own loop now
  checks it.

### 0.14.10 (plugin only)
- Improvement: Identification Database settings now has a real, working
  Download/Update Database button with live progress, instead of a static
  unclickable badge.

### 0.14.9 (plugin only)
- Fix: PLUGIN_ID was still "stash-sense" (v1's id) on the backend side --
  with both plugins installed, every hook-triggered sync silently read
  v1's settings and posted to v1's sidecar.

### 0.14.7 (sidecar) / 0.14.8 (plugin)
- Fix: the "Use GPU" setting was defined and shown but never actually
  read anywhere -- device selection always auto-detected regardless.
- Fix: Operations tab resource badges always showed GPU regardless of the
  setting or real provider availability -- now reflect the actual
  effective resource, with the actual resource used recorded per job.

### 0.14.7 (plugin only)
- Fix: active-job progress bars always rendered full width regardless of
  real progress -- the inline `styles` option was never implemented.

### 0.14.6
- Improvement: changing Detection Resolution now shows a confirmation
  modal, since nothing tracks which resolution produced an existing
  fingerprint -- offers a full reset, apply-going-forward, or cancel.

### 0.14.5
- Fix: frame extraction concurrency scaled with hardware tier, but a
  dedicated benchmark showed no gain past 4 workers (and VAAPI actively
  degrades beyond it) -- now fixed at 4 for all tiers.
- Removed: the dead "Embedding Batch Size" setting, a leftover from the
  old dual-model pipeline that buffalo_l never used.

### 0.14.4
- Fix: fingerprint generation aborted the entire run on the first Stash
  connectivity hiccup -- now retries for up to 5 minutes before giving up.
- Improvement: the resulting error message now names the URL and retry
  duration instead of a generic "Network connection failed".

## 2026-08-22

### 0.14.5
- Fix: the dashboard's sidecar version indicator flagged red on any
  plugin/sidecar version mismatch, not just a genuinely required one --
  now compares against MIN_SIDECAR_VERSION.

### 0.14.4
- Remove: the temporary orange border around the Identify button (a v1/v2
  side-by-side testing aid, no longer needed and mis-targeted anyway).

### 0.14.3
- Fix: with v1 and v2 both installed, only one Identify button ever
  appeared -- both used the same unscoped CSS class. Buttons are now
  tagged per-plugin.
- Fix: AMD GPU VRAM was always reported unknown -- now reads the real
  figure from the amdgpu sysfs interface, correctly classifying tier.
- Improvement: added a manual Refresh button for the Identification
  Database section's counts.

### 0.14.2
- Fix: 0.14.1's plugin-identity fix was incomplete -- the page router's
  startsWith() match also matched the other plugin's route name. Now
  matches this plugin's route exactly.
- Fix: hardware detection only ever probed for NVIDIA GPUs -- always
  reported "No GPU"/cpu-tier on the AMD ROCm reference hardware. Now also
  checks ROCMExecutionProvider + rocminfo.
- Fix: several more UI strings still said "Stash Sense" instead of "Stash
  Sense 2".

### 0.14.1
- Fix: installed alongside v1, this plugin silently read v1's sidecar URL
  and drove v1's backend -- PLUGIN_ID was hardcoded to v1's id. Also fixes
  the displayed plugin name/title missing the "2".

### 0.14.0
- Remove: tattoo and body-proportion identification signals entirely -- a
  real accuracy benchmark found tattoo signal never measurably helped (and
  slightly hurt); body-proportion matching never had any reference data
  populated in the first place.

### 0.13.7
- Change: tattoo signal now nudges match ranking within roughly +-8%
  instead of 0.7-1.5x -- the wider range was found net-negative in a real
  accuracy pass.

### 0.13.6
- Change: the tattoo detector now loads whenever its model file is
  installed, regardless of the Settings toggle -- the toggle now only
  controls whether the data is *used* for matching.
- Change: HSA_OVERRIDE_GFX_VERSION is no longer baked into the ROCm image
  -- now a compose env var, since one value can't fit every AMD card.

### 0.13.5
- Add: the plugin now warns (orange status dot) when the sidecar container
  is running an older version than this JS needs.
- Fix: tattoo detection never actually ran -- startup wrongly required a
  matching index (nothing populates) before even loading the detector.
  Detection and matching are now decoupled.

### 0.13.4
- Add: "Idle Unload Timeout" setting (Settings -> Performance), previously
  hardcoded at 30 minutes; now defaults to 60, settable to 0 to disable.

### 0.13.3
- Add: performers discovered via a non-stash-box catalogue source (e.g.
  seekfans) now render and act correctly as matches -- previously silently
  dropped from the database entirely.

## 2026-08-21

### 0.13.2
- Fix: the image-identify flow now gets the same "Add to Stash + Image" /
  "Add as..." buttons the scene flows already had.

### 0.13.1
- Fix: "Identify current frame"/"Select to identify" now get the same "Add
  to Stash + Scene" buttons "Identify full video" already had.
- Fix: matching now dedupes candidates by resolved performer identity
  before truncating to top-K.

## 2026-08-20

### 0.13.0
- Feature: the scene "Identify" button is now a dropdown with three modes
  (select-region, current-frame, full-video) -- full-video now also uses
  the scene's sprite/thumbnail sheet, previously unused. A step-by-step
  progress list now shows during full-video identify.
- Note: sprite results aren't cached between calls in this release.

## 2026-08-18

### 0.12.24
- Fix: adding a performer from the Identify modal while a scene/image edit
  tab was open always mutated the entity directly, even though that tab's
  own Save could later silently revert the addition. Now stages into the
  open tab's own Performers field instead when one is open, selected by
  verified Stash ID rather than a name match.

### 0.12.23
- Fix: "Start Fingerprinting" let a user re-trigger fingerprint generation
  while a run was already active, most visibly after a page refresh lost
  the in-memory "Started" state. Now checks the job queue directly.

### 0.12.22
- Improvement: the "refresh needed" banner (0.12.21) now only shows when
  there's actually an open edit form with unsaved changes to protect --
  otherwise it reloads automatically.

### 0.12.21
- Improvement: 0.12.18 removed an unsafe auto-select of a newly-added
  performer but left no visual confirmation -- now shows a banner noting
  an open edit form needs a refresh to reflect the change.

## 2026-08-17

### 0.12.19
- Docs: added an explicit public-repository policy to CLAUDE.md (no AI
  trailers, no AI mentions, no internal infra details committed).

### 0.12.18
- Fix: "Add to Scene" could add the wrong performer -- after the real
  mutation, a DOM-typing step to reflect it in an open edit form could
  confirm a different name-colliding suggestion instead. Removed the
  DOM-typing step entirely.
- Fix: the local performer sync hook could permanently fail after a
  delete with "Internal Server Error" due to a Voyager index tombstone
  race -- now treated as a no-op.
- Clarified: merging a performer fires no plugin hook in Stash at all --
  only the scheduled/manual sync task picks up a merge.

### 0.12.16
- Fix: local performer cover images were stored with the sidecar's own
  internal address baked in, breaking whenever the browser reaches Stash
  through a different address. Now stored as a root-relative path.
- Improvement: a local match also linked to a real StashDB id now shows
  both links, not just one.

## 2026-08-16

### 0.12.15
- Fix: local performer matches could be scored using only a single model
  when the other didn't rank the candidate at all, letting a coincidental
  single-model agreement outrank a correct, already-tagged performer. Now
  requires both models to agree, mirroring the main index's handling.
- Improvement: local matches now show a "View local performer" link
  instead of a plain, unlinked badge.

### 0.12.14
- Fix: the local performer sync retry-cache never cleared itself when its
  only cached entry matched the performer currently being synced --
  cleared in memory but never written back to disk.

### 0.12.13
- Fix: local performer sync detected image changes by hashing the URL
  (which includes a cache-busting timestamp bumped on *any* field edit),
  re-embedding on every unrelated metadata change. Now hashes image bytes.

### 0.12.12
- Fix: local performer sync misclassified every performer without a
  custom cover as a failure instead of "no usable image yet" (52 of 1461
  performers on one install). Detected via the URL's default marker.
- Improvement: the sync summary now reports genuine fetch/decode failures
  separately.

### 0.12.11
- Feature: local performer database for identification -- a second, much
  smaller face index built from this Stash instance's own performer
  covers, queried alongside the main database with a distance boost and a
  direct "Add to Scene" action.
- Feature: optional auto-sync via Performer Create/Update/Destroy hooks
  (off by default), with a retry-cache file if the sidecar is unreachable.

### 0.12.10
- Fix: the CPU hardware tier defaulted Frames Per Scene to 30 instead of
  the 60 used by GPU tiers, same class of accuracy-affecting CPU default
  bug as 0.12.8. All tiers now default to 60.

### 0.12.9
- Fix: the Body Proportions/Tattoo Detection Settings toggles had no
  effect -- every caller omitted use_body/use_tattoo, so the endpoint's
  hardcoded True default was always used regardless.
- Improvement: body/tattoo extraction during fingerprinting now always
  runs and caches when models are available, independent of the toggle --
  only *using* the signal for ranking respects it.
- Fix: lazy-loading face recognition models blocked the entire event loop,
  hanging even /health during a cold load. Now runs off the event loop.

### 0.12.8
- Fix: the CPU hardware tier defaulted Detection Resolution to 320 instead
  of the 640 used by GPU tiers -- unlike other CPU-tier defaults, this one
  affects match accuracy, not just speed. All tiers now default to 640.

### 0.12.7
- Feature: when fingerprinting finds zero usable faces, it now retries
  once with the sampling grid shifted by half an interval before accepting
  the scene as having no identifiable faces.

## 2026-08-15

### 0.12.6
- Fix: fingerprint generation's ETA used a lifetime-average rate, going
  stale across a job's distinct phases (e.g. reading "1s remaining"
  through an 11-minute real-work phase). Now a rolling ~30s window.
- Improvement: a "Start Fingerprinting" button now sits next to the
  Missing count on the Settings tab.

### 0.12.5
- Cleanup: removed the dead legacy fingerprint-generator singleton and its
  now-unused progress endpoint/fields.

### 0.12.4
- Fix: the orphaned-fingerprint prune only covered 'complete' rows, missing
  'error' rows for scenes since deleted from Stash.
- Fix: Identification Database stats only fetched once per session instead
  of on every Settings tab switch.
- Fix: hardware detection reported a phantom NVIDIA GPU on hosts with none
  at all -- onnxruntime listing CUDAExecutionProvider only means library
  support, not a real device. pynvml is now the authoritative check.

### 0.12.3
- Fix: a scene that fingerprinted successfully but matched zero performers
  never got marked "complete", permanently showing as "missing" no matter
  how many times generation re-ran (82 scenes on one install). Now saves
  the fingerprint with an empty performer list whenever the pipeline
  actually completes.

### 0.12.2
- Fix: fingerprint generation and two other paginated scene queries had no
  explicit sort, letting pagination order shift on job resume and inflate
  processed-count past total. All three now sort by id explicitly.
- Improvement: Accept All/Dismiss All buttons no longer flash then vanish
  on page load.
- Improvement: Identification Database stats now split into database
  stats vs. this install's own fingerprint coverage; retired the
  no-longer-meaningful "Need Refresh" warning.
- Fix: orphaned scene_fingerprints rows (scene deleted from Stash) were
  never cleaned up, inflating counts and sometimes making "missing" go
  negative (858 orphaned rows on one install). Now pruned on every check.

### 0.12.1
- Fix: Operations/Settings tab panels double-padded their horizontal
  margin against the dashboard container.
- Fix: Identification Database stat boxes didn't size to content on
  mobile, overflowing long text like the "update available" badge.
- Improvement: recommendation type cards on the dashboard are now fully
  clickable and keyboard-operable; removed the redundant "View All" button.
- Improvement: Accept All/Dismiss All buttons are hidden rather than shown
  disabled when there's nothing pending.

### 0.12.0
- Feature: scene fingerprinting now caches the raw face/body/tattoo
  signals it extracts per scene -- a performer-database update no longer
  forces full re-extraction, only the cheap re-matching step, via a new
  use_cache fast path (schema v12).

### 0.11.1
- Fix: the underscore/dash word-wrap fix from 0.9.1/0.9.2 didn't cover the
  Duplicate Scenes detail view, a third renderer sharing the same styles.

### 0.11.0
- Feature: database updates can now apply a small delta package instead of
  re-downloading the full ~2GB release, when a complete delta chain
  exists; falls back to a full download on any gap. Same backup/rollback
  safety as before.

## 2026-08-14

### 0.10.0
- Feature: the database update source repo can now be overridden via the
  DATABASE_UPDATE_REPO env var, letting this fork point at its own data
  repo without re-patching database_updater.py.

## 2026-08-10

### 0.9.2
- Fix: the underscore/dash word-wrap fix from 0.9.1 only applied to the
  Upstream Scene Changes preview, not the Scene Stash-Box Tagger detail
  view (a separate renderer with the same box styles).

### 0.9.1
- Fix: long scene titles/filenames with no spaces now wrap in the Upstream
  Scene Changes preview boxes instead of overflowing the layout.

## 2026-07-28

### 0.9.0
- Feature: Upstream Scene Changes now shows "Local Scene"/"Stash-Box
  Match" preview boxes, matching the Scene Stash-Box Tagger, so a match
  can be visually confirmed even with little local metadata.

## 2026-06-30

### 0.8.1
- Fix: Accept All/Dismiss All buttons are now disabled when there's
  nothing to act on, and wrap to a new line on mobile instead of
  overflowing.

## 2026-06-10

### 0.8.0
- Improvement: debug logging for upstream change-detection jobs is far
  less chatty (dropped low-level httpcore connection traces) and far more
  useful (per-entity outcome lines, retry details with full traceback).
- Feature: job result summaries now report a "N refreshed" count alongside
  "N added" for recommendations that were re-validated in place.

## 2026-06-09

### 0.7.7-beta.1
- Fix: upstream scene changes job was completely broken (TypeError on
  every run, 0 results) -- a missing parameter from the 0.7.6 refactor.

### 0.7.6
- Fix: progress bars for tagger/upstream-change jobs now show the current
  stash-box endpoint name, clarifying why the counter resets per endpoint.
- Feature: the Settings debug log file list now refreshes live (on tab
  switch, every 5s while active, and after toggling debug logging).

### 0.7.5
- Fix: StashBox API timeouts no longer immediately fail jobs -- now
  retried up to 5 times with increasing delays.
- Feature: job history now shows a result summary for completed jobs and a
  short human-readable reason for failures.
- Change: network tasks are now serialised (one at a time) to avoid
  rate-limit bursts and make retries more predictable.

### 0.7.4
- Fix: Duplicate Scenes' resolved/dismissed tab now sorts by recency
  instead of confidence.

### 0.7.3
- Fix: resolved/dismissed recommendations now sort by full datetime
  descending instead of date-only (was time-ascending within a day due to
  text-sorted timestamps).

### 0.7.2
- Feature: "Accept All Changes" for Upstream Scene Changes now processes
  every pending recommendation, including relational changes (performers,
  tags, studio), not just simple field changes -- with full enrichment for
  any newly-created performer/tag/studio.

### 0.7.1
- Fix: restored the "Accept All Changes" button for scenes with only
  simple field changes; relational changes still require individual review.
- Fix: "Accept All Tag/URL/Code Only Changes" no longer false-fails on
  no-op entries where local and upstream values are already identical.
- Fix: accept/dismiss buttons in upstream change lists now wrap on
  mobile instead of overflowing.

### 0.7.0
- Feature: creating a new performer/studio/tag from a stashbox match now
  scrapes and applies the full entity profile immediately, instead of
  leaving it with only name/stash_id. Enrichment failures are logged but
  never fail the creation itself.

## 2026-06-08

### 0.6.0
- Fix: "Delete Match"/"Delete Source" in duplicate scenes now actually
  delete the scene file, respecting the configured trash path.
- UI: clearer delete-confirmation wording, truncated long names, and
  matched preview-card widths in the duplicate scenes detail view.

### 0.5.17
- Enhancement: rejected scene-change recommendations (HTTP 400) now log
  the specific blocking condition at DEBUG level, and all four bulk-action
  buttons log filter stats before processing.

### 0.5.16
- UI: the "Enable Anonymisation?" confirmation modal now has proper inner
  padding and clearer button/description text.

### 0.5.15
- Fix: "Accept All Tag/URL/Code Only Changes" no longer counts a
  scene-since-deleted-from-Stash as a failure -- now deleted from the
  database and reported as "stale removed" instead.
- Enhancement: the debug-logging startup message now includes both
  sidecar and plugin versions.

### 0.5.14
- Enhancement: the debug log now includes the sidecar version, logs every
  incoming HTTP request (method/path/status/duration), and logs key action
  endpoints with relevant entity IDs.

### 0.5.13
- Fix: "Accept All Tag/URL/Code Only Changes" no longer shows a generic
  "accepting failed" with no detail -- failures are now counted
  individually with console detail, and the sidecar logs unexpected
  exceptions with a stack trace. Large recommendation-list fetches (500+
  items) also get a longer timeout.

## 2026-06-06

### 0.5.12
- Fix: 404 error saving the "Normalize Enum Display" setting -- the
  sidecar was missing its settings-persistence endpoints.
- Fix: breast type/ethnicity/eye/hair color no longer display or save in
  ALL_CAPS under enum normalization (they're freetext, not real enums).

### 0.5.11
- Feature: "Accept All URL Only Changes" for Upstream Performer Changes,
  modelled on the existing scene equivalent.

## 2026-06-05

### 0.5.10
- Fix: VAAPI hardware acceleration now actually works on AMD/Intel -- the
  static ffmpeg binary can't dlopen the Mesa VAAPI driver at all. Now uses
  a dynamically-linked system ffmpeg specifically for VAAPI mode.

### 0.5.9
- Fix: VAAPI acceleration failed at startup -- the Docker image was
  missing the VA-API runtime libraries the static ffmpeg binary needs.

### 0.5.8
- Feature: new FFMPEG_HWACCEL env var (cuda/vaapi/none) for
  hardware-accelerated frame decoding, reducing OOM crashes on 4K+/HEVC
  content.

### 0.5.7
- Fix: the Docker image now ships a modern static ffmpeg build instead of
  Ubuntu 22.04's bundled 2021-era one -- better HEVC/10-bit decoding and
  memory efficiency, may reduce OOM crashes on very large files.

### 0.5.6
- Fix: analyzers no longer crash on non-UTF-8 file paths (e.g. Latin-1
  accented filenames) -- the GraphQL client now decodes with errors='replace'.

### 0.5.5
- Fix: fingerprint generation's crash-loop fix now actually survives a
  SIGKILL -- the error record is written before each scene is processed,
  not only inside an exception handler that never runs on SIGKILL.
- Fix: Stop/Force Stop now actually stops the job instead of it being
  immediately re-queued -- user-cancelled jobs are now marked completed,
  not resumed.

### 0.5.4
- Fix: fingerprint generation no longer enters an infinite crash loop on a
  repeatedly-failing scene -- error records are now written for every
  failure type, and skipped on resume.
- Fix: an interrupted job is now correctly re-queued for resumption
  instead of marked "completed".
- Fix: ffmpeg frame-extraction failures now log full stderr at WARNING level.

### 0.5.3
- Fix: the Settings log file browser 500'd (unrecognized dispatch mode).
- Fix: /health and /system/info reported mismatched sidecar versions (a
  missed version bump in main.py).
- Fix: a null recommendation-counts response (sidecar offline) crashed the
  dashboard instead of rendering as empty.

### 0.5.2
- Feature: "Download All Logs" button in Settings -> Diagnostics (zip of
  all debug log files).

### 0.5.1
- Feature: debug log file browser in Settings -> Diagnostics, with
  per-file download/delete and a "Delete All Logs" button.
- Feature: enabling "Anonymise Debug Log" with existing logs present now
  warns before deleting them (to avoid mixing anonymised/non-anonymised
  entries).

### 0.5.0
- Fix: dismissed/resolved recommendation tabs now sort by date/time newest
  first, with the timestamp shown on each card.
- Fix: the top-level tab bar no longer breaks mobile layouts (scrolls
  horizontally instead of wrapping).
- Feature: dashboard counter badges now refresh automatically on tab
  switch.
- Feature: plugin/sidecar versions now shown in the header, with a
  mismatch highlighted in red.
- Feature: new "Anonymise Debug Log" setting redacts names/titles/paths
  from the debug log.

### 0.4.0
- Fix: GitHub API rate-limit errors on Docker crash-loops -- the update
  check now uses a persistent 12-hour cache that survives restarts.
- Feature: "Debug Logging" setting writes verbose logs to a rotating file.
- Fix/Feature: fingerprint generation now supports cursor-based resumption
  with a checkpoint saved every 100 scenes.

## 2026-06-02

### 0.3.0
- Feature: duplicate scenes are now displayed in collapsible groups.
- Fix: removed a redundant "Upstream Changes:" title prefix, and fixed the
  local studio not showing correctly in scene-change recommendations.

## 2026-06-01

- Feature: server-side linked selection replaces a JS cache-driven
  approach, with smarter related-item pre-selection.
- Fix: added stale-scene validation both server- and client-side; restored
  performer-link visibility in existing recommendations; cleaned up
  duplicate-check queries for performance.
- Feature: added a performer-link option to upstream scene change
  recommendations, with alias checking.

## 2026-05-23

- Fix: regression in duplicate-scene sort order (high-confidence now
  correctly sorts at top).

## 2026-05-20

- Fix: hardened force-full-rescan on the manual stash-box tagger job
  against inconsistent state.

## 2026-05-19

- Feature: added studio/performer links on tagger and duplicate detail
  pages.
- Improvement: task progress now more accurately reflects job state;
  tagger skips scenes that already have a stashid.

## 2026-05-18

- Fix: duplicate-scene merge query error causing merge failures.
- Fix: force full rescan on manual tagger run; high-confidence matches now
  sort at top with zero-evidence false suggestions removed.

## 2026-05-17

### 0.2.0
- Feature: support for merged/deleted performers in upstream performer sync.
- Improvement: replacement performers no longer hidden in scene
  recommendations.
- Feature: option to accept all tag-only changes, broadened to include
  URL/code changes.
- Fix: local studio lookup, queue-add progress stall, and null-vs-empty
  comparison false positives.
- Fix: removed an unsupported "add old name as alias" option for scene
  titles (not supported by the Stash API).

## 2026-05-16

- Feature: scene preview/cover/links shown in stash-box tagger details.
- Improvement: high-confidence tagger results now sort to the top, with a
  confidence-scoring explanation.
- Feature: added missing libgles2 dependency for full graphics support.
- Fix: deprecated InsightFace estimate calls updated.
- Feature: modal auto-closes and inserts the performer after adding from
  face recognition; other matches now show details/photos for comparison.
- Fix: false-positive upstream tag changes when values were already
  identical locally.
- Feature: forced re-scan on manual upstream scene change trigger.
- Fix: graceful handling of "tag already exists" errors; added
  stale-entity checks and duplicate-studio fix.
- Improvement: tag suggestions now respect aliases and suppress redundant
  linked local tags.
- Fix: stopped treating performer alias differences as scene changes
  (unsupported by the Stash API).

## 2026-05-15

- Improvement: high-confidence results now sort to top of stash-box tagger
  results; more detail added to the local scene card.

## 2026-05-14

### 0.1.3
- Feature: gender setting for upstream sync field comparisons.
- Feature: stash-box tagger now only matches unlinked scenes, auto-
  dismissing other pending matches for the same scene.
- Improvement: forced re-scan on manual tagger trigger; adjusted upstream
  progress and tagger job state handling.
- Feature: scene preview/cover/links added to tagger detail view.
